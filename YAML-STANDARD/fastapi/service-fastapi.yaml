apiVersion: v1
kind: Service
metadata:
  name: exam-fastapi
  namespace: standard
  labels:
    app: exam-fastapi
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: exam-fastapi
  selector:
    app: exam-fastapi
